# Laku Sitok does not load a root .env file.
# Copy backend/.env.example to backend/.env for server-only values.
# Copy frontend/.env.example to frontend/.env for browser-safe values.
# Never place API keys, service-role keys, or passwords in this root file.
